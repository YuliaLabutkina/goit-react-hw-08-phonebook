import { Loader, One, Two, Three } from './PreLoader.style';

const PreLoader = () => {
  return (
    <Loader>
      <One />
      <Two />
      <Three />
    </Loader>
  );
};

export default PreLoader;
